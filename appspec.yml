version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/react/shopping
    overwrite: yes
file_exists_behavior: OVERWRITE

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu
